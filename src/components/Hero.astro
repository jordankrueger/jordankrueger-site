---
import SocialIcons from './SocialIcons.astro';
---

<section class="hero">
  <div class="container hero-inner">
    <div class="hero-text">
      <h1 class="hero-greeting fade-up">Hiya & welcome! 👋</h1>
      <p class="hero-intro fade-up delay-1">I'm Jordan: Operations nerd, tech translator.</p>
      <p class="hero-sub fade-up delay-2">I help progressive orgs cut through the noise, simplify their systems, and get back to the work that matters.</p>
      <div class="hero-actions fade-up delay-3">
        <a href="/about" class="btn btn-primary">Learn more</a>
        <a href="https://calendly.com/campaignhelp/30min" class="btn btn-outline" target="_blank" rel="noopener">Hire me</a>
      </div>
      <div class="fade-up delay-4">
        <SocialIcons />
      </div>
    </div>
    <div class="hero-image fade-up delay-2">
      <img src="/images/hero-jordan.png" alt="Jordan Krueger" width="512" height="512" />
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(170deg, var(--color-bg-white) 60%, var(--color-bg-warm) 100%);
    padding: var(--spacing-xl) 0;
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-lg);
    align-items: center;
  }

  .hero-greeting {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: var(--spacing-sm);
    line-height: 1.3;
  }

  .hero-intro {
    font-size: 1.2rem;
    font-weight: 500;
    margin-bottom: var(--spacing-xs);
  }

  .hero-sub {
    font-size: 1rem;
    color: var(--color-text-light);
    margin-bottom: var(--spacing-md);
  }

  .hero-actions {
    display: flex;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-md);
  }

  .hero-image img {
    width: 100%;
    max-width: 420px;
    border-radius: var(--radius-lg);
    margin-left: auto;
    display: block;
    box-shadow: 0 8px 32px rgba(217,178,169,0.3);
  }

  @media (max-width: 640px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-greeting {
      font-size: 2rem;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-image {
      order: -1;
    }

    .hero-image img {
      max-width: 280px;
      margin: 0 auto;
    }

    .hero :global(.social-icons) {
      justify-content: center;
    }
  }
</style>
